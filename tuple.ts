// tuple >> array like structure where each element represents some property of a record
const drink : [string, boolean, number] = ["sprite", true, 12];

// type alias
type xyz = [string, boolean, number];

const drinkB : xyz = ["qwerty", false, 0];
const drinkC : xyz = ["abcd", false, -1];